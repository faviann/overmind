# Moraine V1 current behavior for Codex evidence capture

Research for [Adopt Moraine for Codex V1 evidence capture and historical backfill](https://github.com/faviann/overmind/issues/206), checked 2026-08-22.

## Question and method

What is the smallest proof path for using current Moraine as the external Codex evidence/capture substrate, and where does its behavior differ from the milestone's settled requirements?

This note uses only first-party upstream evidence from `eric-tramel/moraine`: default branch `main` at commit [`2fb4aec7a59e9859b8b215eca0810ae9da2ec076`](https://github.com/eric-tramel/moraine/commit/2fb4aec7a59e9859b8b215eca0810ae9da2ec076), the latest stable release [`v0.7.3`](https://github.com/eric-tramel/moraine/releases/tag/v0.7.3), and upstream issue [Stable v0.7.3 CLI installs incompatible mainline plugins](https://github.com/eric-tramel/moraine/issues/669). Confirmed behavior below means it is stated in those docs or implemented/tested in source. Items marked inference are conclusions from those sources or from the operator-supplied local inventory.

The operator reports Codex 0.149.0, 2,558 active rollout files (about 1.697 GB, 66,802 lines), eight archived rollout files (about 8.2 MB, 4,364 lines), and a largest observed JSONL line of about 2.19 MB. The `workstation` LXC has 32 GiB RAM and Docker/Compose; its persistent `~/.codex` is bind-mounted from `/ephemeral/workstation/home/.codex`. The separate `overmind` LXC can also see `/ephemeral`, while its existing Docker stack has only Overmind data/backup mounts. Moraine and Cargo are not currently installed. I did not ingest or alter that corpus.

## Answer in brief

- **No evidence-infrastructure redesign is needed for the V1 proof.** Moraine already supplies a local ClickHouse-backed ingest service, startup backfill, filesystem watches plus periodic reconciliation, raw source-shaped JSON rows with provenance, tolerant Codex `unknown` events, default pre-persistence secret redaction, replay-stable normalized event IDs, a monitor, and read-only MCP search/open tools.
- **For the settled #206 V1 scope, use Moraine's normal local-first path on `workstation`: local ingest, local default ClickHouse, and local MCP/monitor.** This is exactly what `moraine up` is designed to start, keeps source watches beside `~/.codex`, and needs no cross-LXC mount, network credential, route, mirror, or central migration owner. Central aggregation is the immediate follow-up effort, not part of this proof.
- **A local collector plus named mirror is outage-tolerant but is not a complete replication layer.** Its default ClickHouse continues ingesting while the central server is unreachable, and per-host remote checkpoints allow multiple collectors to share a central database. Recovery re-reads the original trace files, however—not the durable local ClickHouse—and routing excludes file-level errors, sessions without a discoverable cwd, and any prefix observed before route resolution. Deleting the source before catch-up can leave a permanent central gap.
- **Do not make an official Moraine container image a proof prerequisite.** Upstream explicitly says its only Moraine Docker image is an unpublished development sandbox and Docker is not a supported end-user runtime. The supported V1 form is host-installed Moraine on `workstation` with its managed ClickHouse. Putting Moraine itself into Compose requires a locally maintained image.
- **Archived Codex rollouts are not covered by Moraine's default source.** Add a second ordinary `codex` harness source for `~/.codex/archived_sessions/**/*.jsonl`; Moraine supports multiple `[[ingest.sources]]`, and its own defaults use multiple sources for one harness where needed. This is configuration work, not a custom harness.
- **The smallest install path should avoid `moraine setup`'s stable/plugin combination for MCP.** Upstream issue 669 confirms that stable v0.7.3 setup fetches incompatible plugin content from `main`. Install the stable CLI for the stack, create/edit the config, and manually register `moraine run mcp` with Codex for dogfooding; alternatively build/install the exact main commit after provisioning Rust. The bug affects guided plugin/MCP registration, not Codex ingestion, ClickHouse, or the direct stdio MCP server.
- **There is one real fidelity boundary to verify, not redesign:** Moraine stores every valid JSON object only while its source line and resulting serialized row fit hard limits. Oversized, malformed, or non-object lines advance the checkpoint and produce `ingest_errors`, but do **not** get a `raw_events` row. The reported 2.19 MB maximum is below the default 8 MiB source-line cap; the proof must still assert zero post-serialization size, parse, and normalization omissions from `ingest_errors` rather than assume corpus completeness.
- **Ordinary restart/reconcile is idempotent at the file cursor, while raw storage is intentionally append-only.** An unchanged file at the committed size returns without emitting; append resumes at byte offset. A path move creates a new checkpoint/raw identity. A crash after a raw insert but before the later checkpoint insert can also re-append raw rows on retry because ClickHouse inserts are staged, not transactional across tables. Neither caveat requires an Overmind deduper for this milestone.

## Smallest proof topology

Current Moraine describes itself as a local trace stack that indexes harness files into ClickHouse and exposes a monitor plus MCP retrieval ([README](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/README.md#L7-L17)). `moraine up` starts three logical pieces on the same host: managed ClickHouse, the ingest service, and one unified backend serving the monitor, HTTP API, Streamable HTTP MCP, and a private Unix socket for stdio compatibility ([quickstart](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/quickstart.md#L68-L96), [shared backend](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/agent-mcp-search/install.md#L178-L199)). The default listeners are loopback (`127.0.0.1:8123` for ClickHouse and `127.0.0.1:8080` for the backend), so Docker and a separate PostgreSQL integration are unnecessary.

That ordinary all-local stack is the selected #206 topology. It is not a special deployment mode: the default backend is constructed from `[clickhouse]`, receives every batch through the always-on default sink, owns the durable checkpoints, and is migrated automatically by `moraine up` ([default backend construction](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-config/src/lib.rs#L1490-L1503), [default sink wiring](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/lib.rs#L286-L333), [quickstart lifecycle](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/quickstart.md#L68-L96)). Source files remain the recovery input, while local ClickHouse is the durable query/evidence store after successful inserts and checkpoints.

Supported install hosts are Linux and macOS; release bundles cover x86-64 and arm64. Managed ClickHouse refuses hosts with less than 2 GiB detectable memory and auto-installs the pinned native ClickHouse build when configured to do so ([platform and memory checks](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/apps/moraine/src/managed_clickhouse.rs#L68-L87), [managed install behavior](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/apps/moraine/src/managed_clickhouse.rs#L748-L803)). The 32-GiB workstation clears those prerequisites. Moraine does not install a login service, so “ongoing automatic” means while `moraine up` is running; after reboot it must be started again ([quickstart](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/quickstart.md#L76-L80)). That is an operational note for V1, not a reason to design permanent deployment infrastructure.

### Version/install choice

As of the research date, the latest published release is v0.7.3, while `main` is nine commits ahead. Upstream issue 669 confirms a coordinated-release bug: v0.7.3's setup installs plugin assets from current `main`; those plugins expect the newer HTTP registration flow, while the stable setup removes the old registration without adding the new one. The issue's confirmed workaround is to pin matching plugin assets, and it also confirms that direct `moraine run mcp` works.

The relevant Codex ingest, normalization, redaction, checkpoint, size-limit, and replay-stable-identity files are byte-identical between tag v0.7.3 and commit `2fb4aec` (verified by local Git blob comparison). Therefore the smallest no-toolchain proof is:

1. `uv tool install moraine-cli` (v0.7.3).
2. Create the runtime config without installing Codex/Claude plugins, then add the two sources below.
3. Start the stack with `moraine up`.
4. For dogfood retrieval, use the stable stdio path directly, e.g. `codex mcp add moraine -- moraine run mcp`. Current Moraine deliberately retains this compatibility path ([shared stdio compatibility](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/agent-mcp-search/install.md#L178-L199), [manual Codex registration](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/agent-mcp-search/install.md#L236-L258)).

Building exact `main` instead is valid but currently requires adding a Rust toolchain; upstream's source path is `cargo build --workspace --locked` followed by `MORAINE_SOURCE_TREE_MODE=1 cargo run -p moraine -- up` ([source install](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/quickstart.md#L217-L235)). That adds setup cost without changing the capture behaviors under test.

### Configuration and deployment choices needed for V1

Only these choices are needed now:

1. Install the stable host bundle/PyPI package on `workstation`; do not create a Moraine container. Use its managed local ClickHouse and default loopback backend unless the proof reveals a concrete host incompatibility.
2. Keep `[clickhouse]` local/default and add the two `~/.codex` sources below. Do not declare `[backends.*]` or `[[routes]]`.
3. Choose a stable `[identity].author` before the initial backfill if the operator can supply it. It may be empty for a local-only install, but named mirroring later requires it, and normalized replay identity incorporates `author`; changing it after history is ingested changes normalized identity ([local/mirror identity policy](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/configuration.md#L87-L107), [replay-stable identity fields](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/sql/033_replay_stable_events.sql#L27-L118)). If no settled value exists, leaving it empty is supported and should be recorded rather than inventing an identity.
4. Run `moraine up` for the bounded proof and register manual local stdio MCP because of issue 669. Record that restart-after-reboot remains manual; service automation and retention policy are later operational work.

### Exact local paths, persistence, and ownership

At defaults, all Moraine processes run as the invoking user and use user-writable paths; the first-party installer defaults to `~/.local/bin` and creates `~/.moraine/config.toml` without `sudo` ([installer paths](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/scripts/install.sh#L11-L32), [config bootstrap](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/scripts/install.sh#L54-L120)). Managed ClickHouse downloads and installs its pinned binary entirely under the configured user paths and starts on loopback; no root-owned host ClickHouse package is required ([user-space managed install](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/apps/moraine/src/managed_clickhouse.rs#L644-L731), [loopback server config](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/config/clickhouse.xml#L1-L28)). Host prerequisites for this path are supported Linux architecture, at least 2 GiB detectable memory, writable home paths, network access for release downloads, `tar` for the ClickHouse archive, and free loopback ports. Moraine itself requests no root capability.

| Default path | Contents | Rebuild treatment |
| --- | --- | --- |
| `~/.codex/sessions` and `~/.codex/archived_sessions` | Original evidence/replay inputs. | Already persistent via `/ephemeral`; must remain readable by the same workstation service user. |
| `~/.moraine/config.toml` | Selected config, including sources, identity, redaction, and local ClickHouse endpoint. | Declarative/user-owned. Safe for dotfiles to own a pinned file or symlink, provided permissions and `$HOME` expansion match the service user. The config selection order prefers an explicit path, then environment, then this home file ([config selection](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/configuration.md#L1-L35)). |
| `~/.moraine/clickhouse/data` | Managed ClickHouse database: raw/normalized evidence, ingest errors, migrations, and durable checkpoints. | **Must persist across LXC rebuild** unless deliberate full re-backfill/data loss is acceptable. The generated server config points ClickHouse's data path here ([runtime construction](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/apps/moraine/src/paths.rs#L18-L48), [ClickHouse data path](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/config/clickhouse.xml#L5-L21)). |
| `~/.moraine/clickhouse/{config.xml,users.xml,tmp,log,user_files,format_schemas}` | Generated ClickHouse config and operational files. | Persisting the whole `~/.moraine` root is simplest; `tmp`, logs, PIDs, and generated XML are not the evidence authority and can be recreated. Keep ownership with the Moraine user. |
| `~/.moraine/mcp-open-cursor.key` | Owner-only key signing MCP continuation cursors. | Preserve to keep outstanding cursors valid across rebuild/restart; replacement does not remove evidence but invalidates old cursors. Moraine creates it mode `0600` and rejects group/other access ([runtime contract](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/configuration.md#L1084-L1122), [key creation](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-mcp-core/src/lib.rs#L349-L390)). |
| `~/.moraine/logs`, `~/.moraine/run` | Logs, PID files, and private `mcp.sock` (socket mode `0600`). | Operational/ephemeral; they need not survive a rebuild. Do not share the socket across users or hosts. |
| `~/.moraine/ingestor` | Configured ingest state directory. | Preserve with the runtime root for forward compatibility, although at this SHA durable file checkpoints are loaded from ClickHouse and no ingest-core call site writes this configured directory ([checkpoint load](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/lib.rs#L184-L205), [configured default](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/config/moraine.toml#L49-L61)). |
| `~/.local/lib/moraine/clickhouse/current` | Downloaded managed ClickHouse binary, version, and checksum. | Cache/reinstallable rather than evidence state; persistence saves a roughly 175 MiB re-download. Runtime defaults and auto-install behavior are documented upstream ([runtime paths](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/configuration.md#L1084-L1122), [auto-install](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/apps/moraine/src/managed_clickhouse.rs#L744-L780)). |
| `~/.local/bin` plus the `uv` tool environment | Moraine command/service binaries and monitor assets. | Reprovisionable from a pinned package/release; appropriate for dotfiles/package management, not evidence persistence. |

Repository split inference: homelab/IaC should guarantee the persistent, correctly owned backing storage for `~/.codex` and at least `~/.moraine/clickhouse/data` (persisting all of `~/.moraine` is operationally simpler but may overlap a dotfiles-owned config). Dotfiles can own the pinned Moraine install, home config, local MCP registration, and an optional user-level service. Upstream installs **no** OS login service, so systemd-user unit/enablement is operator machinery; whether lingering requires a one-time host/root action depends on the LXC's user-session policy, not Moraine ([manual reboot behavior](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/quickstart.md#L68-L80)). Run every Moraine component under one stable workstation UID/home so its files, private socket, and ClickHouse data retain consistent ownership.

### Deferred central topology findings

For the post-V1 aggregation effort, a central host-installed Moraine in the `overmind` LXC could read the shared corpus directly. Configure absolute paths such as `/ephemeral/workstation/home/.codex/sessions/**/*.jsonl` and `/ephemeral/workstation/home/.codex/archived_sessions/**/*.jsonl` rather than `~`, because tilde expansion would otherwise use the `overmind` service user's home. Keep the source tree read-only; current ingest only enumerates, reads metadata, and opens the source files, while checkpoints and state live in ClickHouse and `ingest.state_dir` ([startup enumeration](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/lib.rs#L390-L438), [resume reads](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/dispatch.rs#L1068-L1143)). This is an inference from the documented local-path model, not an upstream-named LXC topology; path readability and ongoing reconciliation would remain proof checks. It is not needed for #206.

Bind/shared-mount notification behavior is deliberately not assumed. Moraine first tries the platform-recommended watcher, falls back to a two-second polling watcher only when native watcher creation fails, and registers the configured local path recursively ([watcher selection](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/watch.rs#L220-L333)). Upstream does not document or test event propagation across this particular LXC/Docker bind arrangement. Confirmed, however, is an independent reconciliation task that re-enumerates every source glob on a 30-second default interval (minimum five seconds) and queues every matching path ([reconcile implementation](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/reconcile.rs#L11-L55), [configuration](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/configuration.md#L863-L892)). Checkpoints make unchanged-file rescans cheap. Therefore missed native events can delay an append by about one reconcile interval but do not require source sync for eventual capture. A proof should append one workstation record, observe whether it appears immediately or after reconciliation, and record the watcher backend/health; silent notification loss is not itself a correctness blocker if reconciliation succeeds.

Moraine does **not** publish an end-user service image or production Compose stack. Its repository's Moraine image identifies itself as “NOT published, NOT a distribution channel,” and the accompanying documentation says Docker is not a supported end-user runtime; supported production installation is the release/PyPI host binary path ([sandbox Dockerfile](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/scripts/dev/sandbox/Dockerfile#L1-L20), [sandbox warning](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/scripts/dev/sandbox/README.md#L79-L85)). Releases contain native `moraine`, `moraine-ingest`, `moraine-monitor`, and `moraine-mcp` binaries plus config/UI assets, not a container image ([release workflow](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/.github/workflows/release-moraine.yml#L69-L86), [bundle contents](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/scripts/package-moraine-release.sh#L123-L144)). Thus a host install on `workstation` is the least custom supported V1 deployment. If a later deployment must remain inside Docker Compose, the only first-party documented runtime image is ClickHouse (`clickhouse/clickhouse-server:25.12.5.44`); Moraine would require an operator-owned image ([remote ClickHouse Docker recipe](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/remote-clickhouse.md#L28-L67)).

The alternatives are supported only in narrower senses:

- **All services on `workstation`:** fully supported, the upstream default, and the selected #206 scope. Source, default database, monitor, and MCP are co-located and loopback-only. The workstation's reported resources clear upstream's managed-ClickHouse floor.
- **Workstation ingest to ClickHouse in `overmind`:** supported using the remote ClickHouse as the *default* backend. Moraine speaks ClickHouse HTTP/HTTPS; the workstation needs the URL, database, and credentials, including migration privileges during initialization and insert/query privileges thereafter. Upstream recommends HTTPS plus firewall/VPN/SSH tunnel and warns against public unauthenticated ClickHouse ([remote prerequisites](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/remote-clickhouse.md#L14-L27), [personal-server configuration](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/remote-clickhouse.md#L69-L146)). `moraine run ingest` is an exposed foreground service, so the watcher can be isolated on the source host ([CLI service split](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/apps/moraine/src/cli.rs#L63-L95), [service choices](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/apps/moraine/src/service.rs#L3-L15)). Unlike a named mirror, remote-default startup fails when ClickHouse is unavailable, so this is not an offline collector topology ([remote failure behavior](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/remote-clickhouse.md#L176-L205)).
- **Named backend/mirror:** useful for future nodes that need a queryable local copy during central outages, but not a transparent substitute for full-corpus replication. Mirror mode always keeps complete history in the default backend and additionally routes matching sessions; exclusive routing is rejected. It requires a discoverable working directory, `[identity].author`, and a pre-migrated remote schema; Moraine never migrates non-default backends ([mirror semantics](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/configuration.md#L301-L317), [durability and limitations](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/configuration.md#L338-L383)). Those semantics add machinery and do not improve the current single-workstation proof's central fidelity.
- **HTTP ingest:** no such first-party surface is present. The documented monitor API is read-only and all canonical routes are `GET`; `/mcp` is retrieval JSON-RPC, not evidence ingestion ([HTTP route matrix](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/monitor-http-api.md#L1-L28), [MCP endpoint](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/monitor-http-api.md#L245-L266)). Filesystem watching therefore remains in the Moraine ingest process that can see the configured paths.

For a later central deployment, one access boundary remains: the unified backend's `/mcp` is intentionally loopback-only. A non-loopback monitor bind requires a guard token that does not authenticate requests and explicitly disables `/mcp`, so exposing it across the LXC network is unsupported/unsafe ([backend bind contract](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/configuration.md#L1030-L1069)). A workstation `moraine run mcp` can instead use its embedded fallback against a remote-default ClickHouse ([stdio fallback](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/agent-mcp-search/install.md#L178-L199), [remote-default semantics](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/remote-clickhouse.md#L1-L12)). None of this networking is needed for local V1 dogfood.

### Reopened audit: central reader versus local collector plus mirror

The following separates what current Moraine guarantees from what would have to be supplied operationally.

| Property | Central shared-tree reader | Local default ClickHouse + central named mirror |
| --- | --- | --- |
| Central unavailable at collector startup | Not applicable: the central process is the collector. When it is down, files accumulate on the shared source tree and default backfill/reconciliation reads retained files after restart. | **Confirmed:** the remote handshake runs in a spawned supervisor, retries every 30 seconds, and failures never affect the default sink. The tee always sends every batch to the awaited default sink while mirror forwarding is bounded/non-blocking ([pipeline separation](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/lib.rs#L286-L333), [router behavior](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/tee.rs#L506-L577), [handshake retry](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/tee.rs#L776-L847)). |
| Recovery source | Original Codex files, against the central default backend's checkpoints. | **Confirmed:** original tracked files, not local ClickHouse. Startup/recovery enumerates each source glob and invokes the same `process_file` using the named backend's checkpoint map ([replay pass](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/tee.rs#L931-L1017)). The local database is an independent retained copy, not a replication spool. |
| Periodic convergence | Default reconciliation re-enumerates source globs every 30 seconds (minimum five), independent of native watcher events. | Mirror convergence has two mechanisms: an unreachable *handshake* retries every 30 seconds; after connection, startup catch-up runs immediately, and a failed/overflowed live sink schedules another replay when it drains. The ordinary reconcile task still scans local sources for default ingest, but it does not copy rows from local ClickHouse to central ([reconcile task](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/reconcile.rs#L11-L55), [recovery transition](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/sink.rs#L243-L275), [catch-up loop](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/tee.rs#L869-L927)). |
| Multiple agent nodes into one central DB | One central reader can enumerate every node path that is simultaneously visible and uniquely configured. No cross-node checkpoint issue arises because it is one writer. | **Confirmed:** migration 018 adds `host` to the checkpoint replacement key, mirror writes stamp it, and mirror loads filter it, preventing identical source paths on different machines from adopting each other's offsets ([migration 018](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/sql/018_checkpoint_host.sql#L1-L20), [host-filtered load](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/lib.rs#L565-L613), [mirror initialization](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/tee.rs#L827-L848)). Caveat: the host key is merely `$HOSTNAME`, falling back to `$USER` then `localhost`; operators must make it unique and stable across nodes ([host identity](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/heartbeat.rs#L1-L7)). |
| Central schema ownership | The central Moraine install owns its default schema through `moraine db migrate` or automatic migration on `moraine up`. | **Confirmed:** agents never migrate a named backend. A designated central/admin Moraine config must treat the central DB as its default and apply every migration before collectors connect; server-behind skew disables each mirror, while an unreachable server retries ([schema ownership](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/configuration.md#L379-L398), [remote migration procedure](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/remote-clickhouse.md#L148-L187)). |
| Coverage of all Codex evidence | Default ingest receives all valid source records regardless of cwd/project routing, subject only to the size/parse fidelity limits below. | **Not complete by contract.** A mirror route is chosen only after a non-empty `session_id` and cwd; no-cwd sessions remain default-only. Raw, event, link, and tool rows for a resolved session mirror, including valid unknown Codex records after they inherit the session cwd. File-level `ingest_errors` never mirror, and an unresolved prefix is checkpointed and never retroactively mirrored after the route pins ([route resolver](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/tee.rs#L296-L417), [backend filtering limitation](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/tee.rs#L419-L471), [Codex cwd inheritance](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/sources/codex.rs#L21-L45)). Current Codex normally puts session metadata first, making the unresolved window small, but “all evidence” cannot be claimed. |
| Source deletion while central is behind | A file deleted before the central default reader sees it is lost unless another configured path still holds the content. The configured active+archive pair reduces move-related risk because an archived rollout is re-read at its new path. | **Confirmed gap:** the source files are the mirror WAL; deletion before catch-up loses rows to central and emits a warning. Local ClickHouse is not consulted to repair it ([documented loss](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/configuration.md#L338-L360), [deletion detection](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/tee.rs#L959-L980)). If a move preserves the full file under the separately configured archive glob, replay can ingest that new path (inference from enumeration and replay-stable normalized identity), but upstream makes no move-aware durability guarantee. |
| Replication-aware retention | None. Moraine relies on the external source tree remaining available until default ingestion. | None. No source-retention lease, acknowledgement-driven cleanup, or local-CH-to-remote replication path exists in current config/implementation. Local tables themselves have no TTL in the base schema, but their persistence does not extend mirror recoverability ([raw/checkpoint engines](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/sql/001_schema.sql#L3-L23), [checkpoint schema](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/sql/001_schema.sql#L196-L208), [file-as-WAL implementation](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/tee.rs#L1-L13)). |
| Deployment increment | One Moraine/ClickHouse installation and two shared-tree sources. Host-installed binaries are upstream-supported; a Moraine container remains custom. | At least two ClickHouse databases now, workstation Moraine services/config, central schema owner, central credentials/network policy, catch-all route policy, stable author and unique hostname, and monitoring of per-backend status. Packaging/container constraints are unchanged. |

**Recommendation limited to issue #206:** prove one local-first producer on `workstation` using Moraine's default local ClickHouse. Validate startup backfill, one live append, restart idempotence, `ingest_errors`, and MCP dogfood locally. Do not configure `backends`, `routes`, remote ClickHouse credentials, a central schema owner, cross-LXC mounts, or a second database in V1. The mirror audit establishes a plausible future extension for disconnected/VPN nodes—local ingest does continue through central outages and checkpoints are host-isolated—but also proves that this is not lossless database replication. Central aggregation should therefore be a fresh follow-up with its route/retention acceptance criteria explicit.

## Exact Codex source configuration

The shipped source covers only `~/.codex/sessions/**/*.jsonl` ([default config](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/config/moraine.toml#L54-L68)). Use two enabled sources with distinct names and the same built-in `codex` adapter:

```toml
[ingest]
backfill_on_start = true

[[ingest.sources]]
name = "codex"
harness = "codex"
enabled = true
glob = "~/.codex/sessions/**/*.jsonl"
watch_root = "~/.codex/sessions"
format = "jsonl"

[[ingest.sources]]
name = "codex-archived"
harness = "codex"
enabled = true
glob = "~/.codex/archived_sessions/**/*.jsonl"
watch_root = "~/.codex/archived_sessions"
format = "jsonl"
```

This is supported by the repeatable `[[ingest.sources]]` model ([configuration](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/configuration.md#L48-L85)); Moraine's own Pi/OMP and Prime defaults demonstrate multiple named sources feeding one harness. Source names matter operationally: the durable checkpoint key is `(source_name, source_file)` ([checkpoint key](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/checkpoint.rs#L4-L29)), while MCP search can filter by the configured `source` separately from `harness` ([MCP source filters](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/agent-mcp-search/interface.md#L93-L115)). Current setup reconciliation matches only the setup-owned source name and preserves differently named custom sources, so later targeted setup need not delete `codex-archived` ([setup matching](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/apps/moraine/src/commands/setup.rs#L1082-L1144)).

For #206, retain these workstation-local `~/.codex/...` paths. A later central shared-reader experiment would replace all four with absolute `/ephemeral/workstation/home/.codex/...` values for both `glob` and `watch_root`. Changing paths creates different raw source identities and checkpoints, another reason not to mix the later aggregation experiment into this proof.

## Backfill, watching, and restart semantics

With `backfill_on_start = true`, ingest loads durable ClickHouse checkpoints, enumerates every enabled source's glob, freezes file/byte targets for progress, starts reconciliation and watcher threads, and queues each startup file ([startup pipeline](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/lib.rs#L390-L470)). New and changed paths are observed recursively by native filesystem notification with a two-second polling watcher fallback; relevant create/data-modify/rename events enqueue the matching file ([watcher](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/watch.rs#L220-L382)). Independently, reconciliation re-enumerates each glob every configured interval (30 seconds by default, clamped to at least five), recovering missed notifications ([reconcile](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/reconcile.rs#L11-L55), [defaults](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/configuration.md#L863-L892)).

For file-backed JSONL, the cursor records inode, source generation, byte offset, and line number. Unchanged files return immediately. Appends seek to the committed offset. Inode replacement or truncation increments the generation and rereads from zero ([file resume logic](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/dispatch.rs#L1068-L1143)). The final checkpoint advances after scanning, including past errors and quarantined lines ([final checkpoint](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/dispatch.rs#L1530-L1560)).

Confirmed implication: an ordinary restart/reconcile does not multiply unchanged active history. Caveat: sink writes `raw_events`, then normalized tables, then checkpoints as separate ClickHouse inserts ([flush order](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/sink.rs#L1256-L1304)). A process/database failure after a successful raw insert but before the checkpoint can reappend raw rows. `raw_events` is a plain `MergeTree`, not a replacing table ([schema](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/sql/001_schema.sql#L3-L23)). This is a failure-window limitation, not continuous multiplication during ordinary operation.

## Raw evidence, provenance, and native session identity

For each valid JSON object, Moraine parses the line and stores a compact reserialization of the entire object in `raw_json`; it is source-shaped and semantically complete but not byte-for-byte source text. The row also includes `source_name`, normalized harness, inferred provider, cwd, source path, inode, generation, line number, byte offset, record timestamp, source discriminator (`top_type`), session ID, a hash, and a raw UID ([normalization envelope](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/normalize.rs#L78-L122), [table schema](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/sql/001_schema.sql#L3-L23)). `author` is also stamped when configured; local-only installs may leave it empty ([identity configuration](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/configuration.md#L87-L107)).

For Codex, `session_meta.payload.id` is the native session ID. Later records inherit it; if the header is unavailable, the adapter falls back to the trailing UUID in the rollout filename ([Codex adapter](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/sources/codex.rs#L12-L45), [filename fallback](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/sources/shared.rs#L746-L756)). This preserves the native session identity across a `sessions` to `archived_sessions` move.

The raw UID intentionally includes `source_file`, generation, line, offset, content fingerprint, and suffix ([raw/provisional UID](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/sources/shared.rs#L930-L950)). Thus the same source record observed at its active and archive paths becomes two durable raw observations. That exactly matches the milestone's accepted V1 limitation.

## Unknown records and hard fidelity limits

Unknown **valid Codex objects are retained**. The raw row is built before adapter routing. An unfamiliar top-level `type` emits an `unknown` normalized event carrying the compact full record, and an unfamiliar `response_item.payload.type` emits an `unknown` event carrying its full payload ([routing](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/sources/codex.rs#L127-L138), [unknown response item](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/sources/codex.rs#L405-L424), [unknown top-level](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/sources/codex.rs#L645-L659)). This satisfies the settled “unknown semantics must not discard otherwise valid raw evidence” requirement.

Three inputs do not receive a raw row:

1. A source line larger than `min(ingest.max_batch_bytes, 8 MiB)` is skipped before parsing; only compact size/provenance metadata enters `ingest_errors` ([source-line guard](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/dispatch.rs#L336-L371), [processing path](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/dispatch.rs#L1328-L1372)).
2. Malformed JSON or a valid non-object produces `json_parse_error` with at most 20,000 characters of fragment and then advances ([parse path](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/dispatch.rs#L1375-L1416)).
3. If any derived `raw_events`, `events`, link, or tool row serializes above ClickHouse's 10 MiB object limit, Moraine drops the whole source line's normalized output—including its raw row—and records only compact size metadata ([serialized-row guard](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/dispatch.rs#L380-L413), [processing path](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/dispatch.rs#L1450-L1488)).

The reported 2.19 MB largest source line is safely below the 8 MiB source guard. Inference: even a roughly doubled JSONEachRow representation remains below 10 MiB, so current corpus size does not reveal a blocker. The proof must still group `ingest_errors.error_kind` and require zero `jsonl_source_line_too_large`, `jsonl_normalized_row_too_large`, `json_parse_error`, and `normalize_error` rows for the selected sources before claiming complete historical coverage.

## Default pre-persistence redaction

Secret redaction defaults to the built-in ruleset; only the home config can honor the explicitly dangerous skip flag, and mirror egress is always redacted ([default config](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/config/moraine.toml#L37-L47)). The tee router applies redaction before handing batches to the default sink ([pre-persistence gate](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/tee.rs#L550-L592)). It scans `raw_json`, normalized text/payload, tool input/output, and error fragments; when raw text changes, it recomputes `raw_json_hash` ([redacted fields](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/redaction.rs#L108-L131), [raw/event mutation](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/redaction.rs#L247-L283)). Built-ins cover common vendor tokens, private keys, JWTs, and entropy-gated generic credential assignments ([rule list](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/crates/moraine-ingest-core/src/redaction.rs#L487-L602)).

Confirmed limitation: this is pattern-based best-effort redaction, not a claim that arbitrary secrets are impossible. That is the accepted default safety boundary for this milestone; leave the skip flag absent and do not redesign it.

## Normalized identity and archive moves

The raw UID is transport-coordinate-sensitive, but the final normalized event UID is semantic. Migration 033 explicitly removes file, generation, line, and offset from canonical replacement identity while retaining them as provenance ([migration intent](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/sql/033_replay_stable_events.sql#L1-L7)). The identity includes author, harness, inference provider, native session ID, event classification/content/tool fields, and a canonical payload that excludes timestamps, cwd, project paths, source references, and other replay coordinates; notably it does not include `source_name` or `source_file` ([identity fields](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/sql/033_replay_stable_events.sql#L27-L118)). The canonical events table is a `ReplacingMergeTree(event_version)` keyed only by `(session_id, event_uid)` ([replacement key](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/sql/033_replay_stable_events.sql#L284-L288)).

Therefore, with one stable/empty `identity.author`, replaying the same rollout at the archive path yields the same normalized UID and one logical event after replacement, while `raw_events` retains both observations. The surviving normalized row's source provenance can change to whichever replacement version wins; raw provenance is the durable place to inspect both observations. Changing `identity.author` between the two ingests would change normalized identity and defeat this collapse, so keep it stable for the proof.

## Search, replay, and raw-evidence dogfood surfaces

The read-only MCP server exposes five tools: `search_sessions`, `open`, `list_sessions`, `file_attention`, and `get_ingest_status` ([interface](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/agent-mcp-search/interface.md#L16-L33)). The useful V1 dogfood sequence is:

1. `get_ingest_status {}` until health, finite coverage, and freshness are true; it reports frozen startup file/byte progress per source without leaking source paths ([status contract](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/agent-mcp-search/interface.md#L35-L50)).
2. `list_sessions` over a broad historical window with `harness: "codex"`, then repeat with `source: "codex-archived"` to prove archived visibility.
3. `search_sessions` for known rare keywords, using explicit tool/reasoning event types when needed; search is BM25 over normalized events, not semantic search ([search contract](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/agent-mcp-search/interface.md#L76-L115)).
4. `open` an event, then its turn/session, following bounded cursors to replay the normalized conversation context ([open contract](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/agent-mcp-search/interface.md#L147-L180)).
5. Inspect `raw_events` and `ingest_errors` directly through ClickHouse or the monitor's bounded table endpoint for source-level assertions. The analytics CLI cannot export raw events—V1 export is normalized `events` only ([export limitation](https://github.com/eric-tramel/moraine/blob/2fb4aec7a59e9859b8b215eca0810ae9da2ec076/docs/export.md#L1-L18)).

MCP is a normalized search/navigation surface, not the proof of raw-evidence fidelity by itself. The raw acceptance checks need SQL/table inspection keyed by `source_name`, `source_file`, `source_line_no`, and native `session_id`.

## Concrete mismatches, blockers, and proof checks

| Finding | Classification | Smallest response |
| --- | --- | --- |
| Default Codex source excludes `archived_sessions`. | Confirmed gap, not blocker. | Add the second `codex-archived` source above. |
| Stable CLI setup installs incompatible current plugins (issue 669). | Confirmed tooling blocker to the guided plugin path only. | Avoid guided plugin install; use stable stack plus manual stdio MCP, or provision Rust and install matching main. |
| Moraine has no published production container image/Compose stack. | Confirmed mismatch only if V1 is forced into Compose. | Install Moraine's host package on `workstation` with managed ClickHouse. Treat a Moraine image as later deployment work, not proof scope. |
| The central backend's MCP endpoint is loopback-only; its non-loopback monitor mode is unauthenticated and omits `/mcp`. | Confirmed deferred-aggregation constraint. | No V1 action; dogfood the workstation-local endpoint. Do not expose a future backend HTTP listener without an authenticated design. |
| Native watcher events across the shared/bind mount are unverified upstream. | Deferred central-reader uncertainty, not a V1 blocker. | No V1 action; the selected watcher reads the workstation-local path. A later shared-reader proof must test reconciliation. |
| Managed ClickHouse evidence defaults under `~/.moraine/clickhouse/data`. | Confirmed rebuild-persistence requirement. | Give this path persistent, Moraine-user-owned storage before relying on V1 capture across LXC rebuilds. |
| No OS login service. | Confirmed operational limitation. | Keep `moraine up` running for the proof and record restart-after-reboot behavior; do not build deployment automation in this milestone. |
| Invalid/non-object, over-8-MiB source lines, and over-10-MiB derived rows are not present in `raw_events`. | Confirmed evidence-fidelity boundary. | Query `ingest_errors`; a nonzero count is a concrete blocker/gap to report, not a reason to predesign storage. |
| Reported current largest line is ~2.19 MB. | Operator observation; no blocker inferred. | Verify post-backfill error counts rather than changing limits preemptively. |
| Plain `raw_events` plus cross-table flush/checkpoint sequence permits duplicate raw rows in failure windows. | Confirmed limitation. | Test ordinary restart (expected stable); document raw duplicate counts if a failure occurs. No Overmind deduper. |
| Archive move duplicates raw path observations but normalized identity is replay-stable. | Confirmed and already accepted. | Move/copy one representative file, wait for reconcile, assert two raw path observations and one `(session_id,event_uid)` normalized event. |
| MCP/export expose normalized projections, not bulk raw replay. | Confirmed surface limitation, not blocker. | Dogfood MCP for inspectability; use ClickHouse/monitor table reads for raw fidelity checks. |

## Recommended next implementation-sized proof

One bounded execution ticket can now install the stable host binaries on `workstation`, configure the two local sources, start the default local stack, run historical backfill, append a live record, restart once, and record the coverage/dogfood report. It should stop and rescope only if `ingest_errors` reveals actual omitted source records, local managed ClickHouse cannot complete backfill within available resources, watcher plus reconciliation fail to observe live appends, or the manual local MCP path fails independently of issue 669. Nothing found here requires reopening the evidence/knowledge boundary, adding Overmind-owned capture, deploying central aggregation, copying the corpus, or designing permanent retention/replication infrastructure.
